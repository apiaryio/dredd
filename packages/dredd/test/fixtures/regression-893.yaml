swagger: "2.0"
info:
  version: "1.0"
  title: Colors API
schemes:
  - http
produces:
  - application/json; charset=utf-8
paths:
  /resource:
    get:
      responses:
        default:
          description: Representation
