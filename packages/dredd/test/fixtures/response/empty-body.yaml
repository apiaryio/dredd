swagger: "2.0"
info:
  version: "1.0"
  title: Colors API
schemes:
  - http
produces:
  - application/json; charset=utf-8
paths:
  /resource:
    get:
      responses:
        200:
          description: Representation
          schema:
            type: object
            properties:
              name: {type: string}
            required: [name]
