swagger: "2.0"
info:
  version: "1.0"
  title: Colors API
schemes:
  - http
produces:
  - application/json
  - text/csv
paths:
  /resource.json:
    get:
      responses:
        200:
          description: Representation
  /resource.csv:
    get:
      responses:
        200:
          description: Representation
          examples:
            text/csv: ""
